# Copy this file to .env and fill in real values.
# NOTE: This repo does NOT load .env at runtime. Supabase credentials are
# injected via Angular environment files (src/environments/environment.ts).
# These names are listed here for reference/documentation only.
SUPABASE_URL=
SUPABASE_ANON_KEY=